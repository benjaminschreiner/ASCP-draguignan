import React from "react";

import {
	BrowserRouter as Router,
	Route,
	Link,
	Redirect,
	withRouter
} from 'react-router-dom';

const Apropos = (props) => (
	<section id="navSide">
		<h2>Apropos</h2>
	</section>
)

export default Apropos;